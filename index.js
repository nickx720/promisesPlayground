const promiseImplementation = require('./promise');
const asyncImplementation = require('./await');

/* promiseImplementation().then((val) => {
    console.log(val);    
}); */

/* asyncImplementation().then((val)=>console.log(val)) */